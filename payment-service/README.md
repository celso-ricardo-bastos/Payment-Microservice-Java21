## 🚀 Como Executar a Aplicação

Para iniciar a aplicação com o perfil de desenvolvimento (`dev`), execute o comando abaixo na raiz do projeto:

```bash
mvn spring-boot:run -Dspring-boot.run.profiles=dev